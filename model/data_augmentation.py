import numpy as np
import os

def data_augmentation(path_to_file):
    # Load the data
    data = np.load(path_to_file)
    # Augment the data
    ### TO DO ###
    augmented_data = data


    # Save the augmented data
    return augmented_data
